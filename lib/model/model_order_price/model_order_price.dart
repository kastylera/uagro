class ModelOrderPrice {
  String? id, cost, createdAt, traiderId, traiderPhone, traiderName;
  bool? isMy;

  ModelOrderPrice({this.id, this.createdAt, this.cost, this.traiderId, this.traiderName, this.traiderPhone, this.isMy});
}
